# Desktop parity matrix

Baseline: Hermes Agent `5122ddd478143a6901bb752cf8ebcd1c5154b6da`, Agent `0.18.2`, Desktop `0.17.0`, verified 18 July 2026. Status is intentionally strict: **implemented** means a real client path exists in this repository; **foundation** is tested domain/transport work without a complete surface; **blocked** identifies a concrete upstream contract gap; **not implemented** is not shipped as decorative UI.

| Desktop capability | Source / backend contract | Android status | Mobile adaptation | Test / limitation | Upstream change |
| --- | --- | --- | --- | --- | --- |
| Remote static-token backend | `electron/connection-config.ts`; `/api/status`, `/api/ws?token=` | Transport only; saved legacy records require reconnect | Dashboard-authenticated onboarding is the supported product path | Static-token gateway tests remain for protocol compatibility; no token onboarding UI | No |
| OAuth remote backend | `dashboard_auth/routes.py`; `/api/auth/ws-ticket` | Blocked | Native PKCE app link, revocable device session | Browser cookies cannot be transferred safely from Custom Tabs | Native OAuth exchange |
| Username/password dashboard auth | `/auth/password-login`; `/api/auth/ws-ticket`; `/api/ws?ticket=` | Implemented | Native credential form, Keystore-backed session cookie, fresh single-use ticket per WebSocket | Contract tests plus physical Samsung smoke against isolated pinned upstream; login and saved reconnect each minted fresh tickets | No |
| Multiple saved backends | Desktop connection config | Implemented baseline | Add/test/select/forget backends; metadata in DataStore and tokens in Keystore | HTTP+WebSocket validation; discovery/QR pairing pending | No |
| Capability/version display | `/api/status`; `session.info.desktop_contract` | Implemented baseline | Dedicated diagnostics surface plus contract warning and version-gated attachment/YOLO controls | Displays app, audited upstream, server, auth, connection, capabilities and session contract; canonical capability document remains desirable | Canonical capability document desirable |
| Session list across profiles | `/api/profiles/sessions` | Implemented | Phone atlas; tablet rail | Real REST path | No |
| Session resume | `session.resume` | Implemented | Durable→runtime identity translation | Reducer tests; real backend pending | No |
| New session | `session.create` | Implemented | One-tap new conversation | Real RPC path | No |
| Streamed text | `message.start/delta/complete` | Implemented | Stable assistant row, no per-token animation | Reducer test | Replay cursor for exact reconnect |
| Structured tool activity | `tool.start/progress/complete` | Implemented baseline | Expandable causal row | Stable `tool_id` reducer test; specialised renderers pending | No |
| Reasoning visibility | `reasoning.delta/available` | Implemented baseline | Collapsed disclosure | Preference/config control pending | No |
| Status and compression | `status.update` | Implemented baseline | Quiet inline technical state | Special lifecycle styling pending | No |
| Approval allow/deny | `approval.request/respond` | Implemented | Blocking prompt bound to source run | Reducer test; permanent-allow confirmation needs hardening | Push action token for background |
| Clarification | `clarify.request/respond` | Implemented | Choice/text blocking prompt | Real RPC path; accessibility UI test pending | Push for background |
| Sudo/secret request | `sudo.request/respond`, `secret.request/respond`, `sudo.expire`, `secret.expire` | Implemented | Blocking password-semantics prompt; value uses non-saveable Compose state, cancel sends the upstream empty response, and matching expiry removes only its own prompt | Reducer request/expiry tests plus two physical Samsung Compose instrumentation tests for masked semantics, unmodified submission and cancellation; no value enters timeline, draft, preferences or logs | No |
| Stop/interruption | `session.interrupt` | Implemented | Stop replaces send during active submission | Real RPC path | No |
| Steer/redirect | `session.steer` | Implemented | Composer switches to explicit steer while a run is active; stop remains separate | RPC path; real backend test pending | No |
| Retry/undo/compress/reset | `session.undo`, `session.close`, `session.create`, `prompt.submit` | Implemented | Confirmed retry/undo/reset actions; retry preserves the last authoritative user text, reset closes the old runtime before creating a clean one | Reducer and pinned result fixtures plus physical reset smoke; undo trims live UI state instead of reloading the pre-undo durable transcript | No |
| Branch | `session.branch` | Implemented baseline | Confirmed session action preserving the visible transcript | Branch identity fixture; real backend test pending | No |
| Session rename/archive/delete/search | `session.title`; `session.delete`; `/api/sessions/search` | Implemented | Confirmed destructive action; immediate recent-list filtering plus debounced profile-scoped FTS message search | Typed route/result tests and physical upstream search/delete smoke; remote search result deletion intentionally requires opening/closing the session first | No |
| Reconnect without duplicates | `session.resume`; events | Foundation; exact in-flight replay blocked | Bounded backoff, then durable-session rehydrate | Replacement-close regression test; no universal stream sequence/replay cursor | Event replay v1 |
| Draft persistence/queue | Desktop renderer state | Draft persistence implemented; queue absent | Debounced backend/profile/session-scoped DataStore draft, cleared only after accepted submit/steer and removed with forgotten backends | Stable hashed key tests plus physical force-stop/relaunch restoration; pending-message queue still absent | No |
| Slash autocomplete/catalogue | `commands.catalog`, `complete.slash`, `slash.exec`, `command.dispatch` | Implemented | Rounded inline palette with grouped catalogue, debounced completions and argument replacement | Typed catalogue/completion/dispatch fixtures; terminal-only commands are filtered, while backend skill and user quick commands remain discoverable | No |
| Model/provider catalogue | `model.options`; `/api/model/options`; `/api/env`; `/api/providers/validate` | Dynamic catalogue, model selection and API-key/custom-endpoint account management implemented | Session picker plus profile-scoped provider cards generated entirely from Hermes metadata | Typed route tests; OAuth/external/SDK provider flows are displayed as server-managed but not yet actionable | Canonical schema desirable |
| Reasoning effort/fast mode/YOLO | `config.get/set`, `session.info` | Implemented baseline | Capability-gated effort/fast controls; session-only YOLO with explicit warning | Protocol fixture; Compose/device tests pending | No |
| File/image/PDF attach | `file.attach`, `image.attach_bytes`, `pdf.attach`, `image.detach` | Implemented baseline | SAF upload, 10 MiB client cap, removable pending chips | Pinned response fixtures; camera/progress/large streaming pending | No for current path |
| File/artifact preview | `/api/files`, `/api/files/read`, `/api/files/download`; desktop managed filesystem bridge | Managed workspace browser implemented; canonical generated-artifact delivery pending | Directory navigation, bounded text/source/raster/PDF viewers, network-isolated HTML WebView, SAF export with cancellation and progress | Typed route, path-encoding, MIME/data-limit and streaming tests plus physical Samsung text/HTML smoke; stale session paths recover to an editable managed path | General remote artifact descriptor desirable |
| Voice STT/TTS | Desktop `hermes.ts`, `voice-playback.ts`, `speech-text.ts`; `/api/audio/transcribe`, `/api/audio/speak`; server `tools.tts_tool.text_to_speech_tool` | Implemented | Press-to-talk or tap-to-lock recording, slide up to lock, slide left to cancel, live level/time feedback, per-reply read-aloud, pause/resume/stop, and Android system output selection | REST/payload/sanitisation tests plus physical Samsung permission, gesture, transcription recovery, real Hermes Edge TTS, audio-focus, output-switcher and cache-cleanup QA; Android requests Hermes-generated audio and never substitutes Android TTS | No |
| Profiles management | `/api/profiles`; `/api/profiles/active`; profile-scoped `session.create`/`session.resume` | Implemented baseline | List/create/rename/delete, sticky default, current-process distinction, start profile-scoped session | Contract test; SOUL/editor and per-profile backend overrides pending | Remote profile gateway override discovery remains desirable |
| Skills installed/hub/manage | REST skills/hub APIs, `skills.manage` | Installed list/search/toggle plus hub featured/search/preview/scan/install/update/remove implemented | Mandatory source review and security verdict before install; blocked scans cannot be overridden | Typed REST route tests; background actions use bounded redacted status polling | No |
| MCP catalogue/config/reload | `/api/mcp/servers`; `/api/mcp/catalog`; `/api/mcp/servers/{name}/test`; `/api/mcp/servers/{name}/enabled`; `reload.mcp` | Configured-server and approved-catalog inspection, backend probes, enable/disable, and current-runtime reload implemented; add/edit/remove/install/OAuth/per-tool filters absent | Profile-scoped searchable lists; Android shows redacted connection metadata, runs probes on Hermes, and reloads only a matching live session/current profile with the upstream confirmation flag; inactive-profile changes apply at next start | Typed REST/cookie fixtures cover list, catalog, probe success/failure and toggle; reload result fixture covers compute-host fields; cloud gate only, with device and real-backend checks deferred | No |
| Toolsets/providers/config | REST config/schema/toolset APIs | Provider settings implemented; toolsets/general config absent | Redacted profile-scoped credential cards with validate-before-save and confirmed removal | Android never persists entered provider values; MCP creation and toolset forms remain omitted | Formal schema improves durability |
| Cron/jobs/runs | REST cron routes | List/create/edit/delete/pause/resume/run-now and bounded run history implemented | Exact schedule editor labels server-side execution/timezone; run rows open their session | Typed REST route test; natural-language schedule helper pending | Push delivery for mobile result notifications |
| Messaging management | Desktop `app/messaging/index.tsx`; `/api/messaging/platforms`, platform update/test routes, `/api/gateway/restart` | Implemented baseline | Profile-scoped searchable platform catalogue, live status/error/home-channel detail, non-persistent Hermes-owned setup fields, enable/disable, confirmed removal, connection test, and separately confirmed gateway restart | Typed route tests plus physical Samsung catalogue/detail/password-semantics/test-result/restart-confirmation QA against the isolated pinned backend; WhatsApp and Telegram guided pairing flows remain server-specific follow-up work | No |
| Agents/Command Center | Desktop `app/agents/index.tsx`, `store/subagents.ts`; `delegation.status`, `delegation.pause`, `subagent.interrupt`, `process.list`, `process.kill` | Implemented baseline | Rounded Command Center shows the live global delegation cap/pause state, active parent-child workstreams, streamed progress/tool/summary details, files/tokens/cost where emitted, matching child-session navigation, and current-session background-process output with confirmed intervention controls | Deterministic event/tree and pinned snapshot tests plus physical Samsung empty-state, live status, pause/resume restoration and session-scoped process-list QA; activity observed during the current Android connection is bounded in memory, while persisted `spawn_tree.list/load`, retry semantics and suspended-app delivery remain follow-up work | Replay/push beneficial; no direct subagent retry RPC |
| Usage/token accounting | session usage + analytics REST | Not implemented | Accessible summaries | Omitted | No |
| Checkpoint/rollback | `rollback.list/diff/restore` | Not implemented | Explicit diff and confirmation | Omitted | No |
| Logs/doctor/security audit/backup | `/api/ops/doctor`; `/api/ops/security-audit`; `/api/actions/{name}/status` | Doctor and security audit implemented; backup/export absent | Bounded status polling and selectable on-device output with defense-in-depth secret/home-path redaction | Typed route tests and redactor tests; server action may continue after the two-minute mobile polling bound | No |
| Notifications/deep links | Desktop native notifications | Blocked for background delivery | Private channels and exact target links | Foreground prompts work only | Device delivery v1 |
| Local Termux runtime | Upstream Termux support | Research only | Detect/link to explicit companion | No cross-app runtime API | Optional Termux companion contract |
| Phone/tablet/foldable layout | Desktop has responsive renderer | Implemented baseline | Master/detail below 840dp, two-pane above | Physical 360dp Samsung QA passed at 100% and 130% text; tablet/foldable hardware QA pending | No |
| Built-in appearance skins | `apps/desktop/src/themes/presets.ts`; client-owned `/skin` | Cosmetic backlog | Native appearance picker using the exact `nous`, `midnight`, `ember`, `mono`, `cyberpunk`, and `slate` preset palettes; `nous` remains the default | Not implemented; deliberately sequenced behind functional parity and safety work | No |
| Keyboard/accessibility/reduced motion | Desktop design system | Foundation | Semantics, labelled actions, reversible container motion | Physical reduced-motion and enlarged-text checks passed; full TalkBack, switch-access, and hardware-keyboard audit pending | No |
